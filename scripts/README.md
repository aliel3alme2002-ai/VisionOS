# Scripts Directory

The `scripts/` directory contains automation, build, setup, and deployment scripts for VisionOS.

## Structure

- **`build/`**: Monorepo compilation and bundle verification scripts.
- **`deploy/`**: Edge node provisioning and cloud deployment helper scripts.
- **`db/`**: Database migration and seed scripts for PostgreSQL.
